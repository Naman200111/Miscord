interface EditMessageModalProps {
  open: boolean;
  onClose: () => void;
}

const EditMessageModal = ({ open, onClose }: EditMessageModalProps) => {
  // return (
  //   );
};

export default EditMessageModal;
